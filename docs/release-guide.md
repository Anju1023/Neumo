# Nuform リリースガイド

このドキュメントでは、Changesetsを使ったバージョン管理とnpm公開の手順を説明します。

---

## 基本的な流れ

```
コード変更 → changeset追加 → PR/マージ → バージョン更新 → npm公開
```

---

## 1. 変更を記録する（changeset追加）

コンポーネントを変更・追加したら、その変更を記録します。

```bash
pnpm changeset
```

### 対話形式で聞かれること

1. **どのパッケージを変更した？**
   - スペースキーで選択、Enterで確定
   - 今は `@anju/nuform` だけなので、それを選ぶ

2. **バージョンの種類は？**
   | 種類 | いつ使う？ | 例 |
   |------|-----------|-----|
   | `patch` | バグ修正、小さな変更 | 0.1.0 → 0.1.1 |
   | `minor` | 新機能追加（後方互換あり） | 0.1.0 → 0.2.0 |
   | `major` | 破壊的変更（後方互換なし） | 0.1.0 → 1.0.0 |

3. **変更内容の説明**
   - 日本語でOK
   - 例: `Buttonコンポーネントにoutlineバリアントを追加`

### 実行例

```bash
$ pnpm changeset

🦋  Which packages would you like to include?
   ◯ changed packages
     ◉ @anju/nuform

🦋  Which packages should have a major bump?
   ◯ @anju/nuform

🦋  Which packages should have a minor bump?
   ◉ @anju/nuform

🦋  Please enter a summary for this change:
   Buttonコンポーネントにoutlineバリアントを追加

🦋  Summary: Buttonコンポーネントにoutlineバリアントを追加

🦋  === Summary of changesets ===
🦋  minor: @anju/nuform

🦋  Is this your desired changeset? (Y/n) Y

🦋  Changeset added!
```

これで `.changeset/` フォルダに新しいファイルが作成されます。

---

## 2. 変更をコミット・プッシュ

changeset ファイルも一緒にコミットします。

```bash
git add .
git commit -m "feat(nuform): add outline variant to Button"
git push
```

---

## 3. リリースする

### 3.1 バージョン更新

すべてのchangesetを消費して、バージョンを更新します。

```bash
pnpm version-packages
```

これにより：
- `packages/nuform/package.json` のバージョンが更新される
- `packages/nuform/CHANGELOG.md` が生成/更新される
- `.changeset/` 内のchangesetファイルが削除される

### 3.2 変更をコミット

```bash
git add .
git commit -m "chore: release @anju/nuform@x.x.x"
git push
```

### 3.3 npm に公開

```bash
pnpm release
```

これで：
1. ライブラリがビルドされる
2. npm に公開される

---

## よく使うコマンド一覧

| コマンド | 説明 |
|---------|------|
| `pnpm changeset` | 変更を記録（changeset追加） |
| `pnpm changeset status` | 未リリースの変更を確認 |
| `pnpm version-packages` | バージョン更新 + CHANGELOG生成 |
| `pnpm release` | ビルド + npm公開 |

---

## Tips

### changesetを追加し忘れた場合

後からでも追加できます：
```bash
pnpm changeset
```

### リリースしない変更の場合

ドキュメント修正など、パッケージに影響しない変更は changeset 不要です。

```bash
pnpm changeset --empty
```

これで「変更はあるけどリリース不要」という記録ができます。

### 複数の変更をまとめてリリース

複数のPRでchangesetを追加しておくと、`pnpm version-packages` 時にまとめてバージョン更新されます。

---

## npm公開の準備（初回のみ）

### 1. npmアカウント作成
https://www.npmjs.com/ でアカウント作成

### 2. ログイン
```bash
npm login
```

### 3. 2FA設定（推奨）
npm のセキュリティ設定で2FAを有効化

---

## トラブルシューティング

### 「Some packages have been changed but no changesets were found」

変更があるのにchangesetがない状態。`pnpm changeset` で追加するか、リリース不要なら `pnpm changeset --empty` を実行。

### 「npm ERR! 403 Forbidden」

- npm にログインしていない → `npm login`
- パッケージ名が既に使われている → package.json の name を変更

### 「npm ERR! 402 Payment Required」

パッケージがスコープ付き（@org/name）でprivateになっている。`.changeset/config.json` の `access` が `public` になっているか確認。
